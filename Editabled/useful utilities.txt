Random String:
openssl rand -base64 5

ln Order of Args:
ln -s existing_target new_name

Space
Only start worrying about space at around 9 digits of bytes. 7 doesn't even _register_.

Server: (look for inet)
ip addr show wlan0

Random CSS colour from JS:
'#'+Math.floor(Math.random()*16777215).toString(16);